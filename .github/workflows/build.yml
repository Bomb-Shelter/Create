name: build
on: [ push, workflow_dispatch ]

jobs:
  build:
    strategy:
      matrix:
        java: [ 21 ]
    runs-on: ubuntu-latest
    env:
      PUBLISH_SUFFIX: snapshots
      MAVEN_USER: ${{ secrets.MAVEN_USER }}
      MAVEN_PASS: ${{ secrets.MAVEN_PASS }}
      WEBHOOK: ${{ secrets.WEBHOOK }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'

      - name: Make Gradle Wrapper Executable
        run: chmod +x ./gradlew

      - name: Build Flywheel
        run: cd flywheel && ./gradlew build && cd ..

      - name: Build Registrate
        run: cd registrate && ./gradlew build && cd ..

      - name: Build Create
        run: ./gradlew buildOrPublish

      - name: Send Build Artifacts To Webhook
        run: |
          JAR_FILE=$(find build/libs/ -name "*.jar" -type f | head -1)
          if [ -n "$JAR_FILE" ]; then
            curl -F "file=@$JAR_FILE" \
                 -F "content=**Create is done!**

            Build completed successfully! " \
                 $WEBHOOK
          else
            echo "No JAR file found in build/libs/"
            exit 1
          fi
